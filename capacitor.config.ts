import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.isitvegan.isitveganphoto',
  appName: 'isitveganphoto',
  webDir: 'build'
};

export default config;
